import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ninja.samyblake.homeapp',
  appName: 'home-app',
  webDir: 'dist/browser',
};

export default config;
